/**
 * test script for canvas-bako
 * Created by kenji-special on 1/23/15.
 */


var test = require('tape').test;

test('equivalence', function(t) {
    t.equal(1, 1, 'these two numbers are equal');
    t.end();
});